# Everything needed to rebuild the data, train the models, run the tests, and
# work in the notebooks. Installs the app runtime as well.
#
#     pip install -r requirements-dev.txt
#
# A hosted deployment installs requirements.txt only.
-r requirements.txt

# ETL: INEP Census extracts are read with DuckDB; the attainment-rate release
# ships as XLSX.
duckdb==1.5.5
openpyxl==3.1.5

# Model selection — only src/train.py imports xgboost, which is why it is not
# a runtime dependency.
xgboost==3.3.0

# Tests and analysis notebooks.
pytest==9.1.1
seaborn==0.13.2
jupyter==1.1.1
ipykernel==7.3.0

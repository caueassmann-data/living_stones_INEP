# Runtime dependencies for the Streamlit app — this is the file a hosted
# deployment installs, so keep it to what app/ actually imports at runtime.
# Development, ETL, training, and notebook dependencies live in
# requirements-dev.txt, which installs this file too.
#
# Pinned to the versions this project is verified to run against. Bump
# deliberately (and re-run `pytest -q` + `python -m src.train --level both`
# afterward), not automatically — a silent minor-version bump in scikit-learn
# can change model output, and a mismatch against the version that produced
# models/*/pipeline.joblib can break unpickling outright.
pandas==3.0.5
pyarrow==24.0.0
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3
matplotlib==3.11.1
streamlit==1.60.0

# NOTE: xgboost is deliberately absent. It is a large wheel, only src/train.py
# imports it, and both trained models are random forests. If a future retrain
# selects XGBoost, the pipeline will not unpickle here —
# scripts/build_deploy_artifacts.py fails loudly on exactly that case, and the
# fix is to add xgboost back to this file.

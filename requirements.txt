# Pinned to the versions this project is verified to run against.
# Bump deliberately (and re-run `pytest -q` + `python -m src.train --level both`
# afterward), not automatically — a silent minor-version bump in scikit-learn
# or xgboost can change model output.
pandas==3.0.5
pyarrow==24.0.0
duckdb==1.5.5
openpyxl==3.1.5
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
matplotlib==3.11.1
joblib==1.5.3
streamlit==1.60.0
pytest==9.1.1
seaborn==0.13.2
jupyter==1.1.1
ipykernel==7.3.0
